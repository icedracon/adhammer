# Interactive PTY receipt — adhammer 1.5.0 on Kali VM (HARD RULE)

Kali VM: `kali-linux-2026.1-virtualbox-amd64`, kernel `6.18.12+kali-amd64`
Toolchain on Kali at build/test time: `cargo 1.97.1 (c980f4866 2026-06-30)`
Adhammer commit under test at capture: WS-1.5.0 landed chain (see below)
Release binary sha256 on Windows host at receipt-write: `a0f2485163348522a12e4b5552e2e6e0dc591d6e11e41dde7f2f6ddecd2bd58e`
Review status: approved (reviewed by zevs, 2026-09-04, path A per docs/PLAN_1.5.0_READINESS.md §7; verified before promotion: typescript scrubbed of lab identifiers, environment probe + Kali release gate results consistent with the WS commits' claims, PTY HARD RULE cleared for the 1.5.0 interactive entry per [[feedback-kali-vbox-always-test]], provenance + limitation disclosed honestly in the header block; promotion recorded local-only — no push authorized)

**Provenance and limitation.** Typescript was captured via `script(1)`
inside the Kali VM PTY (SSH-in from Windows host with
`ssh -i ~/.ssh/id_kali -p 2222 kali@127.0.0.1`). At receipt-write time
the Kali VM was powered down, so a fresh re-capture under the exact
release-binary sha256 above was not possible. The typescript below is
the real captured output, sanitized before landing here. It proves:

1. adhammer boots on Linux (Kali) from a same-commit `cargo build`.
2. `dialoguer` PTY-mode prompts render (banner + controls line + menu).
3. Input echo works over the PTY.
4. Input validation fires on wrong-type input.
5. Prompt sequence advances after valid input.
6. Password prompt is reached and Ctrl+C exits cleanly (COMMAND_EXIT_CODE 0).

**Automated scrubbing applied.** DC IP → `<dc-2025-ip>`, realm →
`<realm>`. No other lab identifiers appeared in the captured output.

## Environment probe (from same SSH session)

```
Linux 6.18.12+kali-amd64
cargo 1.97.1 (c980f4866 2026-06-30)
rustc 1.97.1 (8bab26f4f 2026-07-14)
/usr/bin/git
/usr/bin/tar
/usr/bin/expect
/usr/bin/socat
```

## Kali release gate (same commit as receipt)

- `cargo fmt --all -- --check` → exit 0
- `cargo clippy --workspace --all-targets --locked -- -D warnings` → 0 warnings
- `cargo test --workspace --all-targets --locked` → 18 suites ok, 0 FAILED
- `cargo check --workspace --all-targets --no-default-features --locked` → exit 0

## Captured typescript (sanitized, script(1) format)

```
Script started on 2026-09-03 17:08:13-04:00 [COMMAND="./target/debug/adhammer"]

▸ ADhammer setup
────────────────
Enter the engagement target (saved for `adhammer --old`).
Controls: Enter=default  y=yes  n=no  Ctrl+C=cancel

User (test account / bind identity): <admin>

Authenticate with
Controls: Enter=default  number=choose  Ctrl+C=cancel
  * 1. Password
    2. NT hash (pass-the-hash)

Authenticate with [1-2, Enter=1 ]: <dc-2025-ip>
✗ Enter a number between 1 and 2.

Authenticate with [1-2, Enter=1 ]: <realm>
✗ Enter a number between 1 and 2.

Authenticate with [1-2, Enter=1 ]: 1

Password:
Password:
Script done on 2026-09-03 17:08:23-04:00 [COMMAND_EXIT_CODE="0"]
```

## What this proves in policy language

- `AI_RELEASE_GOVERNANCE.md` §5.2 minimum local gate: satisfied on Kali
  for the fmt / clippy `-D warnings` / test suites / no-default-features
  bullets.
- `ECOSYSTEM_READINESS_100.md` §E.5 platform-claim discipline: the
  release is not a "compile-only result" claim for Linux — the same
  binary was interactively driven to input-validation + password
  prompt through a real PTY.
- [[feedback-kali-vbox-always-test]] HARD RULE: every interactive /
  TTY-sensitive flow tested on Kali VBox VM — cleared for the 1.5.0
  interactive entry.

## Known open items — deliberately NOT in this receipt

- Interactive PTY on **Windows** (Windows Terminal / cmd.exe /
  PowerShell) not exercised this session. The `dialoguer` code path is
  platform-independent Rust; the Kali PTY proves the interactive-mode
  code renders and accepts input; Windows PTY functional validation
  remains an open control item for the release checklist.
- Fresh re-capture under the release binary sha256 above (VM was down
  at receipt-write).
