# Live-validation evidence — adhammer 1.5.0

Receipt-write date: 2026-09-04
Workspace HEAD at receipt write: `2b4664d` (workspace version `1.5.0`)
Release binary path: `target/release/adhammer.exe`
Release binary sha256: `a0f2485163348522a12e4b5552e2e6e0dc591d6e11e41dde7f2f6ddecd2bd58e`
Review status: pending

**Provenance and limitation — read first.** Unlike the 1.4.10 receipts
(`docs/receipts/1.4.10__*.md`) which were captured from same-session
fresh runs, the per-verb outputs below were captured across the 1.5.0
development session between the WS commits below and consolidated here at
receipt-write time. The lab DCs went unreachable (subnet timeout) at
receipt-write, so a fresh re-run under the release-binary sha256 above
was not possible without a VM restart. The evidence below is truthful,
matches the WS commits' claims, and was sanitized before landing in
this file — but the review discipline "same-day fresh run under the
release binary" is a delta this receipt records rather than hides. Fresh
per-DC receipts under the release binary can be added post-tag once the
lab subnet returns.

**Automated scrubbing applied**: real DC IPs, hostnames, domain, admin
username, admin password, recovered GPP plaintext, and any short-hex key
material were replaced with role-tokens (`<dc-2025-ip>`, `<dc-2019-ip>`,
`<realm>`, `<admin>`, `<admin-pw>`, `<gpp-plaintext>`, `<domain-sid>`).
Manual review is mandatory before promoting review_status to approved.

## Summary — 10 new 1.5.0 workstreams

| WS | Commit | Live-validated | Verdict |
|---|---|---|---|
| WS-FOUNDATION-DNS-HANDROLL | `c6c1398`, `569a6ff` | `<realm>` SRV records resolved via hand-rolled RFC 1035 codec | ✓ |
| WS-FOUNDATION-BLACKBOX-CLI (`run`) | `8109367` | DNS discovery on `<realm>` returned `<dc-2025>` | ✓ |
| WS-WEB-FP (`enum web`) | `3a54718` | `/certsrv` on `<dc-2025-ip>` flagged as ESC8 relay surface | ✓ |
| WS-BLACKBOX-COMPOSE (`run --web`) | `8cb3198` | Per-DC web hits chained from `run` output | ✓ |
| WS-FOUNDATION-NULLBIND (`enum nullbind`) | `38c5dc2` | 2025 → `\samr` refused (`0xc0000022`); 2019 → null session refused (`0xc000006d`) | ✓ |
| WS-BB-RPCNULL (`enum rpc-null`) | `8cd526b` | 2025 → srvsvc/wkssvc/lsarpc all ANON EXPOSED; 2019 → null refused | ✓ |
| WS-BB-SHARES (`enum shares --anon`) | `92557b7` | 2025 → 6 shares listed anon (SYSVOL, NETLOGON, CertEnroll + admin$); 2019 → null refused | ✓ |
| WS-BB-HOST (`enum host --anon` + `run --deep`) | `c99acaf` | 2025 → single-session posture matrix; 2019 → null refused | ✓ |
| WS-SYSVOL-ANON (`enum sysvol`) | `dbe72d3` | 2025 anon → `0xc0000022` ACCESS_DENIED (hardened, expected); 2025 authed as `<admin>` → 2 GPP `cpassword`s recovered end-to-end, plaintext written only to 0600 `--dump` artifact, never to stdout | ⚠︎ anon-happy-path not observed on hardened lab; wire path proven via authed run |
| WS-COERCER (`attack coerce --scan-all`) | `c2a8177` | 2025 → 5-vector matrix, all handled gracefully with exact wire detail (`spoolss` RPC timeout, `\lsarpc` BIND ctx reject result=2, `\efsrpc` + `\FssagentRpc` pipe-not-found `0xc0000034`, `\netdfs` BIND_NAK reason=8), no vector fired → "hardened against all four families" — no lab cruft (DFS root not added because DFSCoerce did not fire) | ✓ |
| WS-HASHGLASS (`attack roast` annotation) | `5b292f0` | `hashglass::identify` standalone-probed against all three roast format families: `$krb5tgs$23$` → mode 13100 (conf 0.99), `$krb5asrep$23$` → mode 18200 (conf 0.99), `$krb5tgs$18$` → mode 19700 (conf 0.99). End-to-end `attack roast` on lab DC blocked env-side (no LDAPS listener + BF-1 correctly refused plaintext bind). | ⚠︎ probe-proven; e2e env-blocked |
| WS-DEPS-BANS (hashglass wildcard pin) | `ff00d31` | `cargo deny check bans` was RED without pin, GREEN after `{ path = "…", version = "0.1" }` | ✓ |

## Per-DC per-verb captured output (sanitized)

### DC-2025 (Windows Server 2025, `<dc-2025>`, `<dc-2025-ip>`) — session-captured

**`enum nullbind --host <dc-2025-ip>`** — 2025 refused anon SAMR:
```
{"host":"<dc-2025-ip>","anonymous":false,"hardened":true,
 "detail":"SamrConnect2: protocol: SamrConnect2 failed (NTSTATUS 0xc0000022)"}
```

**`enum rpc-null --host <dc-2025-ip>`** — 2025 exposed srvsvc + wkssvc + lsarpc anonymously:
```
== <dc-2025-ip> — anonymous RPC ==
  null session established.
  \srvsvc  NetSessionEnum   → 0 session(s) [ANON EXPOSED]
  \wkssvc  WkstaUserEnum    → 0 user(s) [ANON EXPOSED]
  \lsarpc  LsarOpenPolicy   → OK [ANON EXPOSED]
```

**`enum shares --anon --host <dc-2025-ip>`** — 2025 exposed 6 shares anon (incl. CertEnroll, corroborating ESC8):
```
== <dc-2025-ip> — anonymous shares ==
  null session established · NetrShareEnum → 6 share(s):
    ADMIN$           disk [admin$]  — Remote Admin
    C$               disk [admin$]  — Default share
    CertEnroll       disk           — Active Directory Certificate Services share
    IPC$             ipc  [admin$]  — Remote IPC
    NETLOGON         disk           — Logon server share
    SYSVOL           disk           — Logon server share
  ** 3 non-admin share(s) listable anonymously [ANON EXPOSED] —
     this DC permits null-session NetrShareEnum. Harden: RestrictNullSessAccess=1.
```

**`enum host --anon --host <dc-2025-ip>`** — unified single-session posture:
```
  null session established.
  \samr    users            → refused
  \srvsvc  NetSessionEnum   → 0 [ANON EXPOSED]
  \srvsvc  NetrShareEnum    → 6 share(s) [ANON EXPOSED]  (same 6 as above)
  \wkssvc  WkstaUserEnum    → 0 [ANON EXPOSED]
  \lsarpc  LsarOpenPolicy   → OK [ANON EXPOSED]
  ** 3 non-admin share(s) listable anonymously — DC permits null-session NetrShareEnum
     (RestrictNullSessAccess=0).
  detail:
    - samr enum: protocol: SamrConnect2 failed (NTSTATUS 0xc0000022)
```

**`enum sysvol --anon --host <dc-2025-ip>`** — 2025 refused anon SYSVOL walk (secure posture; wire code proven via authed run below):
```
== <dc-2025-ip> — anonymous SYSVOL (SYSVOL) ==
  SYSVOL not readable anonymously — DC hardened (this is the secure posture).
  detail:
    - list SYSVOL: SMB2 status 0xc0000022 for command 0x0005
```

**`enum sysvol --host <dc-2025-ip> --user <admin> --domain <realm> --password env:ADHAMMER_PASSWORD`** — authed walk (proves QUERY_DIRECTORY + read_file wire code end-to-end):
```
== <dc-2025-ip> — authenticated SYSVOL (SYSVOL) ==
  2 GPP cpassword(s) recoverable by any authenticated user [MS14-025]:
    <realm>\Policies\{31B2F340-016D-11D2-945F-00C04FB984F9}\USER\Preferences\Groups\Groups.xml
        account=(unknown)  password=[REDACTED]
    <realm>\Policies\{31B2F340-016D-11D2-945F-00C04FB984F9}\MACHINE\Preferences\Groups\Groups.xml
        account=(unknown)  password=[REDACTED]
  ** remediation: GPP passwords are decryptable by anyone (public MS key).
     Remove cpassword from SYSVOL and rotate the affected accounts.
```
Recovered plaintext went ONLY to a `--dump <path>` 0600 secret artifact
via `adhammer_core::write_secret_artifact`; stdout showed only
`[REDACTED]` markers. The `<gpp-plaintext>` values are not reproduced
in this receipt.

**`attack coerce --scan-all --host <dc-2025-ip> --domain <realm> --user <admin> --password env:ADHAMMER_PASSWORD --listener <in-subnet-bogus-ip>`** — 5-vector matrix; no vector fired (hardened DC):
```
== coercion scan-all → <dc-2025-ip> ==
  [-] PrinterBug    \spoolss       RPC TCP read header timed out
  [-] PetitPotam    \lsarpc        protocol: BIND presentation context rejected: result=2, reason=1
  [-] PetitPotam    \efsrpc        SMB2 status 0xc0000034 for command 0x0005
  [-] DFSCoerce     \netdfs        protocol: BIND_NAK reject_reason=8
  [-] ShadowyCoerce \FssagentRpc   SMB2 status 0xc0000034 for command 0x0005
  no vector fired — this DC is hardened against all four coercion families over
  the pipes tried (or the services are disabled).
```
No DFS root added — DFSCoerce did not reach `NetrDfsAddStdRoot`. Lab-clean.

### DC-2019 (Windows Server 2019, `<dc-2019-ip>`) — session-captured

Every anon SMB verb refused at the SMB layer (`SESSION_SETUP` returned
`0xc000006d STATUS_LOGON_FAILURE`), so no SAMR/srvsvc/wkssvc/lsarpc/
NetrShareEnum was reachable. The verbs correctly reported this as
"hardened against anonymous SMB" instead of erroring out:

- `enum nullbind`: `null session refused — DC hardened against anonymous SMB. detail: SamrEnumerateUsers: SMB2 status 0xc000006d for command 0x0001`
- `enum rpc-null`: `null session refused — DC hardened. detail: null session refused: SMB2 status 0xc000006d for command 0x0001`
- `enum shares --anon`: `null session refused — DC hardened against anonymous SMB.`
- `enum host --anon`: `null session refused — hardened against anonymous SMB.`
- `enum sysvol --anon`: `null session refused — DC hardened against anonymous SMB.`

## Interactive PTY receipt — Kali VBox VM (HARD RULE cleared)

See `docs/receipts/1.5.0__kali_pty.md` for the sanitized typescript
captured via `script(1)` on `kali-linux-2026.1-virtualbox-amd64`
(`ssh -i ~/.ssh/id_kali -p 2222 kali@127.0.0.1`), which shows the
`adhammer` interactive banner rendering, the dialoguer prompt sequence
(user → target → auth method), input echo, input validation firing
correctly on wrong-type input (`✗ Enter a number between 1 and 2`), and
the password prompt reached before clean Ctrl+C exit (COMMAND_EXIT_CODE
0).

## Static + governance gate status at the release binary HEAD

Same-HEAD (`2b4664d`) local gate on Windows host at receipt-write:

- `cargo fmt --all -- --check` → exit 0
- `cargo clippy --workspace --all-targets --locked -- -D warnings` → 0 warnings
- `cargo test --workspace --all-targets --locked` → 18 suites ok, 0 FAILED
- `cargo check --workspace --all-targets --no-default-features --locked` → exit 0
- `cargo +1.88 check` (declared MSRV) → exit 0
- `cargo doc --workspace --no-deps --locked` → 0 warnings
- `cargo audit --no-fetch` → 306 deps scanned, only documented ignore `RUSTSEC-2023-0071` (rsa 0.9 Marvin; rationale in `.cargo/audit.toml`)
- `cargo deny check advisories bans licenses sources` → all four green
- `cargo package -p <crate>` × 12 crates → all pack (6–78 files each)
- `scripts/check_release_governance.py` → green
- `scripts/check_validation_ledger.py` → green (82 rows across 10 public surfaces)

Kali on the same commit (`2b4664d`): fmt / clippy `-D warnings` / test
18 suites / no-default-features all green; interactive PTY captured
(see companion file).

## Known open items — deliberately NOT in this receipt

- WS-COERCER live-fire against a DC where a vector would succeed (all
  four families are hardened on both lab DCs by design). Coverage is
  "handled gracefully across all 5 wire failure modes".
- WS-HASHGLASS end-to-end against a real live `attack roast` run — env
  gap (no lab LDAPS listener, BF-1 correctly refuses plaintext bind).
  `hashglass::identify` proven standalone across the 3 relevant format
  families.
- WS-SYSVOL-ANON happy path with a legacy `RestrictAnonymous=0` DC —
  lab has none; anon path proven only via the "correctly refused"
  outcome, and the wire code (QUERY_DIRECTORY + read_file) proven via
  the authenticated walk.

These are lab-availability limitations, not code gaps.
