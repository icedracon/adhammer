[package]
name = "adhammer-sysvol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "ADhammer SYSVOL — adapter mapping the standalone `gpo` crate's GPP/GptTmpl results onto ADhammer findings."
repository.workspace = true

[dependencies]
adhammer-core = { workspace = true }
gpo = { path = "../../../gpo" }
