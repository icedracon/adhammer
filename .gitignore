/target
**/*.rs.bk
*.pdb

# private keys from shadow-cred generation
*.key.pem

# Python venv / pip artifacts (reference/oracle tooling only — never vendored)
*.whl
*.egg-info/
__pycache__/
*.pyc
.venv/
venv/

# credential caches / dumps produced at runtime
*.ccache
.ccache/
*.kirbi
*.pcap
*.pfx
*.csr
*.stub
*.issued.pem

# local run artefacts — never commit
auto-report.md
adhammer-report.*
report.json
report.html
report-summary.txt
reports/
esc1.crt
esc1.crt.key.pem
*.crt
.env
.env.*
