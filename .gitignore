/target
**/*.rs.bk
*.pdb

# local scratch reports from live-lab runs — never tracked. Every field in a
# real report (IP, hostname, account, DACL topology) is lab-identifying.
adhammer-pentest-report.*
adhammer-report-*.html
adhammer-report-*.md
auto-report.md
scan-report.*
release-scan.*
release-auto.*
adh-*.crt
adh-*.pfx

# agent workspace + tool traces (per-session, may contain lab endpoints)
.agents/
.playwright-cli/

# private keys from shadow-cred generation
*.key.pem

# Python venv / pip artifacts (reference/oracle tooling only — never vendored)
*.whl
*.egg-info/
__pycache__/
*.pyc
.venv/
venv/

# credential caches / dumps produced at runtime
*.ccache
.ccache/
*.kirbi
*.pcap
*.pfx
*.csr
*.stub
*.issued.pem

# local run artefacts — never commit
auto-report.md
adhammer-report.*
adhammer-pentest-report.*
adhammer-pentest-report-*
*-rebuilt.html
report.json
report.html
report-summary.txt
reports/

# private development notes and evidence — never commit
.agents/
CLAUDE.md
bench/*.log
target-*/
.local-install/
.playwright-cli/
video-work/
patches/
lab/validation-*/
meta.json
*.exe
esc1.crt
esc1.crt.key.pem
*.crt
.env
.env.*

# Internal planning and agent state — never publish.
.agents/
CLAUDE.md

# Benchmark transcripts can contain credentials, hashes, and target details.
bench/*.log

# Local build, test, recording, and patch artifacts.
target-*/
*.exe
.local-install/
.playwright-cli/
video-work/
patches/
lab/validation-*/
meta.json
